# ==============================================
# WEALTH FLOW APPLICATION CONFIGURATION
# ==============================================
# This file serves as a template for your environment variables.
# Copy this file to '.env' and adjust the values for your environment.

# ==============================================
# SECURITY & AUTHENTICATION
# ==============================================
# JWT Secret Key
# CRITICAL: Change this to a long, random string for production.
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=dev-secret-do-not-use-in-production-change-me

# ==============================================
# FRONTEND CONFIGURATION (Build Time)
# ==============================================
# URL for the API.
# - Local Development: http://localhost:3001/api (points to local backend)
# - Production (Docker): /api (relative path, handled by the same server)
VITE_API_URL=http://localhost:3001/api

# ==============================================
# BACKEND CONFIGURATION (Runtime)
# ==============================================
# The port the application will listen on
PORT=3001

# CORS Origin
# - Local Development: http://localhost:5173 (Vite default)
# - Production: Your domain name or container host
CORS_ORIGIN=http://localhost:5173

# Database Path
# Default: ./db/wealth.db (Relative to server/ directory in default config)
# Docker: /app/server/db/wealth.db
DATABASE_PATH=./db/wealth.db

# Token Expiry Duration
# Examples: 1h, 1d, 7d
JWT_EXPIRY=7d

# Node Environment
# Options: development, production
# Set to 'production' in your deployment environment
NODE_ENV=development
